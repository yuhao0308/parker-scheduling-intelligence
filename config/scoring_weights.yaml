weights:
  overtime_headroom: 0.45    # Sean: "top of the priority" — biggest financial lever
  clinical_fit: 0.22         # short- vs long-term unit training match
  float_penalty: 0.13        # protect home-unit staffing + new hires
  proximity: 0.05            # tiebreaker only — not a client-confirmed signal
  seniority: 0.08            # tenure / clinical experience
  equity: 0.05               # surface under-used staff (dormancy boost)
  willingness: 0.02          # historical acceptance — deliberately low per spec

thresholds:
  max_relevant_distance_miles: 30
  max_candidates_returned: 10
  new_hire_months: 6         # tenure threshold for amplified float penalty
  seniority_saturation_years: 10  # tenure where seniority signal saturates to 1.0
  dormancy_threshold_days: 30     # per-diem idle >30 days triggers equity boost

clinical_fit_scores:
  exact_match: 1.0           # home unit or cross-trained for target typology
  subacute_to_lt: 0.8        # Short-Term-trained covering Long-Term — acceptable
  lt_to_subacute: 0.0        # Long-Term-only covering Short-Term — severe penalty

float_penalty_values:
  home_unit: 0.0             # no penalty — target is home unit
  same_typology: 0.3         # different unit, same type (LT→LT or subacute→subacute)
  cross_typology: 0.6        # different unit, different type
  new_hire_multiplier: 1.5   # amplify penalty for staff < new_hire_months tenure
